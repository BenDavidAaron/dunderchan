<div class="post-list">
    {% for post in posts %}
    <div class="card mb-4">
        <div class="card-header">
            {{ post.id }} - {{ post.author_hash }} : {{ post.title }}
        </div>
        {{ post.content }}
    </div>
    {% endfor %}
</div>